# 本体规则功能全栈实现计划

## 一、数据库设计

### 1. 主表: ontology_rules
```sql
CREATE TABLE ontology_rules (
    id VARCHAR(64) PRIMARY KEY,
    rule_category VARCHAR(32) NOT NULL COMMENT '规则类别: CREATE_OBJECT, UPDATE_OBJECT, DELETE_OBJECT, CREATE_LINK, DELETE_LINK, FUNCTION_RULE',
    function_name VARCHAR(128) COMMENT '函数/方法名',
    interface_type VARCHAR(16) NOT NULL COMMENT '接口性质: DUBBO, RESTFUL',
    request_method VARCHAR(8) COMMENT '请求方式: GET, POST, PUT, DELETE (仅RESTful时使用)',
    interface_url VARCHAR(512) COMMENT '接口地址',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

### 2. 子表: ontology_rule_params
```sql
CREATE TABLE ontology_rule_params (
    id VARCHAR(64) PRIMARY KEY,
    rule_id VARCHAR(64) NOT NULL COMMENT '关联规则ID',
    param_direction VARCHAR(8) NOT NULL COMMENT '参数方向: INPUT, OUTPUT',
    param_name VARCHAR(64) NOT NULL COMMENT '参数名',
    param_type VARCHAR(32) COMMENT '类型',
    is_required TINYINT DEFAULT 0 COMMENT '是否必填: 0-否, 1-是',
    description VARCHAR(256) COMMENT '说明',
    sort_order INT DEFAULT 0 COMMENT '排序',
    INDEX idx_rule_id (rule_id)
);
```

## 二、后端实现

### 1. Entity 类
- `/backend/src/main/java/com/ontology/entity/OntologyRule.java`
- `/backend/src/main/java/com/ontology/entity/OntologyRuleParam.java`

### 2. Mapper 接口
- `/backend/src/main/java/com/ontology/mapper/OntologyRuleMapper.java`
- `/backend/src/main/java/com/ontology/mapper/OntologyRuleParamMapper.java`
- `/backend/src/main/resources/mapper/OntologyRuleMapper.xml`
- `/backend/src/main/resources/mapper/OntologyRuleParamMapper.xml`

### 3. Controller
- `/backend/src/main/java/com/ontology/controller/OntologyRuleController.java`
  - `GET /api/ontology-rules` - 列表查询
  - `POST /api/ontology-rules` - 新增规则
  - `PUT /api/ontology-rules/{id}` - 更新规则
  - `DELETE /api/ontology-rules/{id}` - 删除规则

## 三、前端实现

### 1. 导航栏改造
- 修改 `frontend/src/components/Layout.tsx`
- 将"动作类型"改为可展开的父菜单
- 添加"动作类型"和"本体规则"两个子项

### 2. 新增页面
- 创建 `frontend/src/pages/OntologyRules.tsx`
  - 规则列表表格（支持按类别筛选）
  - 新增/编辑弹窗表单
  - 入参/出参动态列表
  - 删除确认

### 3. API 客户端
- 修改 `frontend/src/api/client.ts` 添加规则相关 API

### 4. 路由集成
- 修改 `frontend/src/App.tsx` 添加页面路由

## 四、实施顺序

1. 创建数据库表（执行SQL）
2. 创建后端 Entity 类
3. 创建 Mapper 接口和 XML
4. 创建 Controller
5. 修改前端导航栏（支持展开）
6. 创建前端页面组件
7. 添加前端 API 客户端
8. 集成路由并测试